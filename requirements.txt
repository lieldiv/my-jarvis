# Pinned to the exact versions this build was developed and tested against
# (captured 2026-08). Unpinned requirements meant a fresh `pip install` on a
# new machine could silently grab a newer, untested release of any of these
# — including ones with breaking API changes (the Google/MSAL/Groq SDKs move
# fast) — and fail in ways that have nothing to do with this app's own code.
# To intentionally upgrade one package, bump it here and re-test the
# feature(s) it touches rather than just deleting the pin.

flask==3.1.3
groq==1.0.0
edge-tts==7.2.8
requests==2.32.3
python-dotenv==1.1.0
gunicorn==23.0.0

# --- Windows desktop automation (pyautogui pulls in Pillow + pyscreeze
# transitively, but Pillow is pinned explicitly too since vision_action.py
# imports it directly — an implicit transitive dependency for something
# imported by name in our own code is a portability trap waiting to happen
# the moment pyautogui's own dependency list changes upstream).
#
# CLOUD DEPLOY: commented out for this build. JARVIS_DESKTOP_TOOLS=false
# means the app never calls into these at runtime, and pyautogui/pygetwindow
# won't *install* cleanly on a headless Linux container anyway (no X11
# display). The guarded-import pattern in app.py/vision_action.py already
# handles them being absent gracefully — no code change needed to remove
# them, just uncomment below if this folder is ever run on Windows again.
# pyautogui==0.9.54
# pygetwindow==0.0.9
# Pillow==11.2.1

# --- Process inspection (kill_process tool) -----------------------------
psutil==7.0.0

# --- Google Calendar + Gmail ---------------------------------------------
google-auth==2.40.0
google-auth-oauthlib==1.3.1
google-api-python-client==2.169.0

# --- Microsoft Outlook/365 (Graph API) ------------------------------------
msal==1.37.0

# --- Browser push notifications (reminders, real device notification not
# just email) -- pulls in py-vapid (VAPID key handling) and http-ece
# (payload encryption) as transitive deps automatically.
pywebpush==2.1.2

# Note: get_current_info (tavily_service.py) needs no new dependency here —
# Tavily's API is plain REST, called via the `requests` package already
# pinned above.

# --- Persistent database (users.py) --------------------------------------
# Render's free tier has no persistent disk — a local sqlite3 file gets
# wiped on every restart/spin-down (see render.yaml's own note), which is
# exactly why tasks/reminders were "randomly" vanishing. Turso is a hosted,
# free (no card), SQLite-compatible database; this package's Connection
# object implements the same DB-API surface (.execute/.commit/.fetchone/
# .fetchall) as stdlib sqlite3, so users.py's _connect() only needs to pick
# which one to open, not rewrite any query. Falls back to local sqlite3
# automatically when TURSO_DATABASE_URL isn't set (local dev unaffected).
libsql==0.1.11

# --- Timezone-aware "current time" for the LLM (app.py's LOCAL_TZ) --------
# stdlib zoneinfo needs an IANA tz database; not every base Linux image
# ships one. This pip package is zoneinfo's documented fallback source.
tzdata==2025.2

# --- TLS trust (see cert_bootstrap.py) ------------------------------------
# Pinned explicitly (not just relied on as a transitive dep of requests/
# edge-tts) because cert_bootstrap.py reaches into certifi's internals
# (certifi.where / certifi.core.where) at import time — a certifi release
# that renames/removes those would break cert_bootstrap.py silently.
certifi==2025.4.26
