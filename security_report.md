# דוח בדיקת אבטחה — תזכורות, Push Notifications, זיהוי דיבור, וביקורת OWASP מלאה

**תאריך:** 2026-08-31 (עודכן פעמיים באותו יום)
**היקף:** התחיל כבדיקה ממוקדת (תזכורות/Push/זיהוי דיבור), הורחב לביקורת OWASP Top 10 מלאה של כל המערכת — ראו חלק ב׳ למטה.
**שיטה:** בדיקה ידנית, קובץ-אחר-קובץ, בליווי בדיקות אימות בפועל (לא רק קריאת קוד) — כולל שחזור מקומי של באגים לפני תיקון, ובדיקות יחידה (unit tests).

> **הערה על שיטת העבודה:** הפקודה המובנית `/security-review` נכשלה בגלל באג בתבנית שלה עצמה (`git diff origin/HEAD...` חסר את הצד השני של הטווח) — וידאתי ש-`origin/HEAD` מוגדר נכון בריפו (`git remote set-head origin main`) והבאג נשאר, כלומר זה לא תקלה בהגדרות הריפו אלא בפקודה המובנית עצמה. בעקבות זאת בוצעה בדיקה ידנית + סוכן ייעודי (`.claude/agents/security-auditor.md`) במקום.

---

## סיכום מנהלים

**חלק א׳ (תזכורות/Push/דיבור):** נמצאה בעיה אחת בחומרה נמוכה — תוקנה. בנוסף תועד תיקון משמעותי יותר במנגנון ה-Push (מ-VAPID) שבוצע באותו יום.

**חלק ב׳ (ביקורת OWASP מלאה של כל המערכת):** נמצא **ממצא קריטי אחד אמיתי** — הכלי `write_and_test_code` איפשר לכל משתמש מחובר להריץ קוד פייתון עם גישה לכל סודות השרת ולקובץ מסד הנתונים (שמכיל טוקנים של גוגל של **כל** המשתמשים, בטקסט גלוי). **הוסר לגמרי מהמערכת** (לא רק תוקן חלקית) — ראו חלק ב׳.

| # | בעיה | חומרה | סטטוס |
|---|------|--------|--------|
| 1 | ולידציה חסרה על weekday/hour/minute ב-`request_update_reminder` | נמוכה | ✅ **תוקן** |
| 2 | מפתח VAPID לא נטען נכון (`Vapid.from_string()` על PEM מלא) | גבוהה (זמינות, לא סודיות) | ✅ **תוקן** |
| 3 | כפילות tag בהתראות Push (test מול תזכורת אמיתית) | נמוכה (בלבול, לא אבטחה) | ✅ **תוקן** |
| 4 | `write_and_test_code` — הרצת קוד עם גישה לכל הסודות + ל-users.db | **קריטית** | ✅ **הכלי הוסר לגמרי** |
| 5 | `/api/inbox/send-reply` בודק הרשאה אחרי פענוח הבקשה + אין הגבלת גודל בקשה גלובלית | בינונית | ✅ **תוקן** |
| 6 | `computer_use` מסתמך על ספרייה חסרה במקום בדיקת דגל מפורשת | נמוכה | ✅ **תוקן** |

---

# חלק א׳ — תזכורות, Push, זיהוי דיבור

---

## 1. ולידציה חסרה — `PATCH /api/reminders/<id>`

**קובץ:** `productivity_service.py`, שורה 710 (לפני התיקון)
**חומרה:** נמוכה — לא ניתן לניצול לגניבת מידע/הרשאות, אבל תקלה אמיתית.

### התיאור
`request_update_reminder()` בדקה `0 <= final_weekday <= 6` וכו' מבלי לוודא קודם שהערכים הם בכלל מספרים. קריאת PATCH ישירה (למשל דרך curl, לא דרך ה-UI) עם ערך כמו `{"weekday": "abc"}` הייתה גורמת ל-`TypeError` לא-מטופל:

```
TypeError: '<=' not supported between instances of 'int' and 'str'
```

השרת החזיר 500 גולמי במקום תגובת JSON נקייה כמו בכל שאר הראוטים באפליקציה.

### למה זו לא פרצת אבטחה חמורה
- לא נחשף Stack trace — `FLASK_DEBUG` כבוי כברירת מחדל ולא מוגדר ב-`render.yaml`, כך שב-production מוצג רק שגיאת 500 גנרית.
- אין דרך לגרום לכך שהקריאה תיגע בנתונים של משתמש אחר — הבדיקה קורסת *לפני* שהיא מגיעה לשכבת ה-DB.
- דורש session מאומת תקף (`session.get("user_id")`) — לא נגיש לגורם אנונימי.

### התיקון (בוצע ונבדק)
```python
try:
    final_weekday, final_hour, final_minute = int(final_weekday), int(final_hour), int(final_minute)
except (TypeError, ValueError):
    return {"status": "error", "message": "That doesn't look like a valid day or time, sir."}
```

**אימות בפועל:** הרצתי תרחיש אמיתי מול מסד נתונים זמני — יצירת תזכורת אמיתית, ואז קריאה עם `weekday="abc"` ועם `minute="oops"` — שתיהן חזרו כעת עם הודעת שגיאה נקייה במקום לקרוס, ועדכון תקין (`weekday=2, hour=15, minute=30`) עדיין הצליח כרגיל.

**קומיט:** `868a056` — נדחף ל-`origin/main`.

---

## 2. באג בטעינת מפתח ה-VAPID (Push Notifications)

**קובץ:** `push_service.py`
**חומרה:** גבוהה מבחינת **זמינות** (ההתראות פשוט לא עבדו בכלל) — **לא** בעיית סודיות/הרשאות.

### התיאור
הקוד מסר לפונקציית `webpush()` מחרוזת PEM מלאה (כולל `-----BEGIN PRIVATE KEY-----`) כערך `vapid_private_key`. הספרייה הפנימית (`pywebpush`) מנתבת מחרוזת כזו ל-`py_vapid.Vapid.from_string()` — פונקציה שלא יודעת להתמודד עם כותרות PEM בכלל: היא פשוט מנסה לפענח base64url על **כל המחרוזת**, כולל הכותרות המילוליות, ומייצרת בייטים פגומים. זו הייתה הסיבה האמיתית לשגיאת `"Could not deserialize key data... ASN.1 parsing error"` שחזרה שוב ושוב לאורך כל הדיבוגים הקודמים בפרויקט הזה (תיקוני קידוד ה-Render, ה-TTL, ה-manifest — כולם היו תיקונים אמיתיים, אבל אף אחד מהם לא היה הבאג הזה).

### אימות
שוחזר מקומית — נוצר זוג מפתחות טרי, בוצע תהליך העטיפה/פענוח המדויק כמו בקוד, והתקבלה **בדיוק** אותה שגיאת ASN.1. לאחר התיקון (בניית אובייקט `Vapid` באמצעות `cryptography.load_pem_private_key` במקום מסירת מחרוזת גולמית) — אומת שנחתמה JWT אמיתית, ושבוצעה קריאת HTTP אמיתית לשרתי FCM של גוגל שהתקבלה בהצלחה (תגובת 410 לגיטימית על מכשיר בדיוני, לא שגיאת המפתח).

### למה זו לא בעיית סודיות
המפתח הפרטי עצמו מעולם לא נחשף — הבעיה הייתה בפורמט שבו הוא *הועבר בזיכרון התהליך* לספרייה, לא בדליפה שלו החוצה.

**קומיט:** `fc9f633` — נדחף ל-`origin/main`.

---

## 3. כפילות Tag בהתראות Push

**קובץ:** `push_service.py`, `app.py` (Service Worker), `daily_briefing.py`
**חומרה:** נמוכה — פגם בחוויית משתמש, לא באבטחה.

### התיאור
כל התראה (גם הודעת בדיקה וגם תזכורת אמיתית) השתמשה באותו tag קבוע (`jarvis-reminder`) מול מערכת ההפעלה. תג משותף יכול לגרום להתראה ישנה (שעדיין לא נסגרה) "להתחלף" בתצוגה עם אחת חדשה — נצפה בפועל כתזכורת שמציגה תוכן של הודעת בדיקה קודמת.

### התיקון
כל תזכורת מקבלת tag ייחודי (`jarvis-reminder-<id>`), הודעות בדיקה מקבלות tag נפרד (`jarvis-test`) שלעולם לא יתנגש עם תזכורת אמיתית.

**קומיט:** `45df9da` — נדחף ל-`origin/main`.

---

## תחומים שנבדקו ונמצאו תקינים

| תחום | מה נבדק | ממצא |
|------|---------|------|
| **הרשאות (IDOR)** | כל שאילתת תזכורת מסוננת לפי `user_id` הן ברמת ה-SQL (`WHERE user_id = ?`) והן ברמת ה-Python (`list_active_reminders`) לפני שהיא מגיעה לעריכה/מחיקה | ✅ אין דרך לגעת בתזכורת של משתמש אחר, גם עם ID ידוע |
| **SQL Injection** | כל שאילתה במסד הנתונים | ✅ שימוש עקבי ב-placeholders (`?`), אין שרשור מחרוזות |
| **XSS** | כל שימוש ב-`innerHTML` בקובץ ה-HTML (25 מקומות) | ✅ תוכן ממשתמש/API תמיד דרך `.textContent`/`escapeHtml()` |
| **CSRF** | הגדרת עוגיית ה-session | ✅ `SameSite=Lax` + `Secure` ב-production |
| **דליפת סודות ב-git** | היסטוריית git של `render.yaml`/`.env.example` | ✅ אין עקבות של מפתח VAPID אמיתי, רק שם המשתנה |
| **לוגים** | כל `logger.info/error` בקבצים שנגעתי בהם | ✅ הלוג האבחוני של המפתח מדפיס רק אורך + 12 תווים ראשונים/אחרונים (בעיקר מהכותרת הקבועה של PEM, לא מהמפתח בפועל) |
| **הרשאת-על / Privilege Escalation** | `refuse_if_elevated()`, `resolve_safe_path()` | ✅ קיימים ופעילים (לא שונו בסשן זה, אומתו כרלוונטיים) |

---

## המלצות להמשך מחלק א׳ (לא דחופות)

- שקול הוספת `MAX_CONTENT_LENGTH` גלובלי ל-Flask כדי להגביל גודל בקשה (הגנה כללית מפני resource exhaustion) — לא ספציפי לתזכורות/Push, קיים בכל האפליקציה.
- אין הגבלת אורך על טקסט תזכורת (`text`) — לא בעיה חדשה מהסשן הזה, אלא דפוס קיים מאז `set_reminder` המקורי.

---

# חלק ב׳ — ביקורת OWASP Top 10 מלאה של כל המערכת

**שיטה:** סוכן ייעודי (`.claude/agents/security-auditor.md`, נבנה במיוחד לצורך זה) עבר מודול-מודול על כל הקוד — לא רק על השינויים מהסשן הזה — ובדק כל אחד מול קטגוריות OWASP Top 10 הרלוונטיות לו.

## הממצא הקריטי: `write_and_test_code`

**מה זה היה:** כלי שנתן ל-JARVIS לכתוב ולהריץ קוד פייתון אמיתי לפי בקשה בשפה טבעית ("תכתוב סקריפט ש...").

**הבעיה:** הקוד רץ עם **כל משתני הסביבה הסודיים של השרת גלויים לו** (מפתחות API, סוד ה-OAuth של גוגל, מפתח ה-VAPID) — כי `subprocess.run()` בלי פרמטר `env` מעביר את כל הסביבה של התהליך האב לתהליך הילד, כברירת מחדל. בנוסף, הקוד יכול היה לקרוא כל קובץ בדיסק בנתיב מוחלט — כולל `users.db`, ששומר את ה-tokens של גוגל (קלנדר+ג'ימייל) של **כל** המשתמשים שהתחברו אי פעם, בטקסט גלוי לגמרי. שילוב של השניים = כל משתמש מחובר (גם אם לא זדוני בכוונה, סתם ביקש "תכתוב לי סקריפט שמדפיס את משתני הסביבה") יכול היה לחשוף את כל סודות המערכת ואת חשבונות הגוגל של כולם.

**מה עשינו, בשני שלבים:**
1. **תיקון ביניים (מיידי):** הגבלת משתני הסביבה שהקוד המורץ יכול לראות (allowlist קטן וטכני בלבד — לא סודות). נבדק בפועל: קוד רגיל (import, קבצים זמניים) עדיין עובד, וסוד מזויף לא נראה יותר.
2. **החלטה סופית (לאחר שהצגתי לך את האפשרויות):** **הסרת הכלי לגמרי** מהמערכת — כולל מחיקת `self_healing.py`, הסרת הכלי מרשימת היכולות של ה-LLM, ועדכון כל התיעוד (`CLAUDE.md`, `INTEGRATION.md`) כדי שאף אחד לא יחבר את זה מחדש בטעות בלי לדעת למה זה הוסר.

**סטטוס:** ✅ **הכלי לא קיים יותר במערכת** — לא בגישה חלקית, אלא בוטל לחלוטין. אומת: `write_and_test_code` לא מופיע ברשימת הכלים שנשלחת ל-LLM ולא בטבלת הביצוע.

## ממצאים נוספים מהביקורת המלאה

| חומרה | תחום | תיאור | סטטוס |
|-------|------|-------|--------|
| בינונית | `/api/inbox/send-reply` | בדק הרשאה *אחרי* שכבר פענח את גוף הבקשה, ולא הייתה הגבלת גודל בקשה גלובלית (`MAX_CONTENT_LENGTH`) באפליקציה כלל | ✅ **תוקן** — סדר הבדיקה הוחלף (הרשאה קודם), ונוסף `MAX_CONTENT_LENGTH=24MB` גלובלי. אומת בפועל: בקשה אנונימית (גם ענקית) מקבלת 401 בלי שהגוף נגע בכלל; בקשה מאומתת של 30MB מקבלת 413 נקי; בקשה תקינה עדיין עובדת עד הסוף |
| נמוכה | `computer_use` | הסתמך על כך שספרייה מסוימת (`Pillow`/`pyautogui`) לא מותקנת בענן, במקום בדיקת דגל מפורשת כמו הכלים האחרים | ✅ **תוקן** — נוספה בדיקת `DESKTOP_TOOLS_ENABLED` מפורשת, זהה לזו שיש ל-`open_application`/`close_application`. אומת בפועל
| מידע | תזמון כפול | אם `gauntlet_main.py` (כלי דסקטופ נפרד) ירוץ אי פעם על אותה מכונה כמו שרת חי, שני thread-ים מתזמנים יכולים להתחיל — ישלחו תזכורות כפולות. לא רלוונטי ל-Render (לא מריץ את gauntlet) | מידע בלבד |
| מידע | `drive.readonly` | הרשאת גוגל (scope) מבוקשת אך לא בשימוש בשום מקום בקוד — כדאי להסיר לפי עקרון least-privilege | המלצה |
| מידע | `google_login.py` | סקריפט ישן שלא תואם יותר לגרסה המרובת-משתמשים — יקרוס אם ירוץ, לא באג אבטחה | מידע בלבד |

## מה נבדק ונמצא נקי (ברמת המערכת כולה)

- **הרשאות בכל 30 ה-routes:** כל route שקורא/כותב נתונים אישיים בודק `session["user_id"]` — אומת אחד-אחד.
- **הזרקת פקודות מערכת:** `open_application`/`close_application` משתמשים ב-`subprocess` בלי `shell=True` בשום מקום.
- **מחשבון:** `calculate()` משתמש ב-AST whitelist, לא ב-`eval()` גולמי.
- **כותרות אבטחה:** `X-Frame-Options`, `X-Content-Type-Options`, הסרת כותרת `Server`.
- **CSRF ב-OAuth:** `state` ו-PKCE `code_verifier` מאומתים דרך ה-session לפני חילופי טוקנים מול גוגל.
- **Debug mode:** כבוי כברירת מחדל, לא מוגדר ב-`render.yaml`.
- **הזרקת כותרות HTTP:** שם קובץ מצורף במייל מנוקה מ-`\r\n"` לפני כניסה לכותרת `Content-Disposition`.
- **SSRF:** כתובות ה-API החיצוניות (Tavily, Yahoo Finance) קבועות בקוד, לא נבנות מקלט משתמש.
- **XSS בפרונט:** נבדקו כל 25 מקומות ה-`innerHTML` בקובץ — כולם משתמשים בטקסט סטטי או עוברים דרך `escapeHtml()`.
- **דליפת סודות ל-git:** `git log --all --full-history` על `.env`/`users.db`/`jarvis.log`/`.certs` — לא נמצא כלום, מעולם.

## מה הביקורת לא כיסתה (בכנות)

- CVE-ים בספריות חיצוניות — אין גישה לבסיס נתונים חי של פרצות ידועות; הגרסאות ב-`requirements.txt` רק "נראות סבירות".
- תשתית האחסון עצמה (Render/Northflank) — מחוץ לגבולות קוד האפליקציה.
- כל דבר שדורש מכשיר/דפדפן אמיתי כדי לבדוק בפועל (למשל אמינות Push על אייפון אמיתי).
